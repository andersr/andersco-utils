{
  "name": "@andersco/utils",
  "version": "1.0.0",
  "description": "TS utils I regularly re-use",
  "types": "dist/index.d.ts",
  "main": "dist/index.js",
  "scripts": {
    "prepare" : "npm run build",
    "postversion" : "git push && git push --tags",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Anders Ramsay",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.6.2"
  }
}
